# include <bits/stdc++.h>
# define report(args...) return printf(args), void()
# define N 2010
struct pair{
	int to,val;
};
std::vector<pair> e[N<<1];
void _() {
	int n,m,k,i,x1,y1,x2,y2;
	scanf("%d%d%d",&n,&m,&k);
	for(i=1;i<=k;i++) {
		scanf("%d%d%d%d",&x1,&y1,&x2,&y2);
		if(y1<y2) add(x1,y1,)
	}
}

int main() {
	int T=1;
	scanf("%d",&T);
	while(T--) _();
	return 0;
}