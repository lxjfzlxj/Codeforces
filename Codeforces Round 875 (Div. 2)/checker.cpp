# include <bits/stdc++.h>

int main() {
	int cas=0;
	while(1) {
		system("gen");
		system("E");
		system("std");
		printf("Case %d:\n",++cas);
		if(system("fc E.out std.out")) break;
		puts("Correct");
	}
	puts("Wrong Answer!");
	return 0;
}
